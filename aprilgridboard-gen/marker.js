class AprilMarker
{
	constructor(aprilTagFamily, margin, startID, cols, rows, tagSize, tagSpace) {
		this.aprilTagFamily = aprilTagFamily;
		this.margin = margin;
		this.startID = startID;
		this.cols = cols;
		this.rows = rows;
		this.tagSize = tagSize;
		this.tagSpace = tagSpace;
	  }
}
